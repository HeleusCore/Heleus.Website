@{
    ViewData["Feedback"] = await Model.ReadMarkDownAsync("feedback");
}

<section class="bg-light u-content-space">
    <div class="container">
        <div class="text-center w-md-50 mx-auto mb-8">
            <markdown markdown=@ViewData["Feedback"]></markdown>
        </div>
    </div>
</section>
